---
interface Props {
	value: string
	label: string
}

const { value, label } = Astro.props
---

<div class="text-center">
	<div class="mb-2 text-4xl font-bold text-primary">{value}</div>
	<div class="text-sm text-muted-foreground">{label}</div>
</div>
